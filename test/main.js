var should = require('should');
var Encoder = require('../lib/main');

describe('Encoder', function() {
    describe('with no arguments', function() {
        //it('returns an empty array', function() {
        //    var result = sleepsort();
        //    result.should.eql([]);
        //});
    });
});
